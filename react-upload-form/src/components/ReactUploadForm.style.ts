import { css } from "@emotion/react";

export default css`
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background-color: var(--test);
    width: fit-content;
    padding: 25px;
`;